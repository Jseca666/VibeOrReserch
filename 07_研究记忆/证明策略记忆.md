# 证明策略记忆

## 可复用策略

- 对离散名义分布的 Wasserstein 球，先把运输计划按原子分解，再处理每个原子的条件分布。
- 推前集合等价通常需要分别证明两个包含方向。
- 从低维/latent 空间 lift 回原空间时，必须检查可测选择或近似选择。

## 常见失败模式

- 把 \(W_1\) 与 \(W_2^2\) 成本混用。
- 忽略 \(\Xi\) 中不在 \(\Phi(\mathcal W)\) 的点。
- 用“显然存在 \(w(\xi)\)”跳过 measurable selection。
