# EVO-20260502-009：启动 NXT-20260502-007 小规模脚本实现准备

## 1. 轮次信息

| 字段 | 内容 |
|---|---|
| 轮次 ID | EVO-20260502-009 |
| 日期 | 2026-05-02 |
| 输入触发 | 用户同意进入 NXT-20260502-007 |
| 主任务类型 | T4 计算实验与反例验证 |
| 关联 claim | C-009/C-011 |
| 调用角色 | 离散DRCC验证实验员；大规模MILP算法专家；反例搜索员；实验与Benchmark副导师；研究债务协调员 |
| 使用技能 | `experiment-plan`；手工项目角色审查 |
| 本轮边界 | 脚本实现准备；不生成正式 benchmark，不升级 claim，不关闭 `阻塞` 或 `claim阻断` 债务 |

## 2. 启动前记忆检索

| 记忆来源 | 读取结论 | 本轮影响 |
|---|---|---|
| `07_研究记忆/实验记忆.md` | SageMath 不作为默认依赖；NXT-007 运行前不写入成功策略 | 脚本采用纯 Python 标准库，不依赖 Sage/SciPy/MILP 求解器 |
| `07_研究记忆/证明策略记忆.md` | 离散 DRCC 先固定坏集距离，再对照分数背包、对偶、网格 epigraph；finite fiber 反例优先 | 脚本实现两个模块：离散 DRCC 对照与 finite fiber A/B smoke search |

## 3. 本轮产物

| 产物 | 位置 | 处理结果 |
|---|---|---|
| 小规模验证脚本 | `06_计算实验/scripts/nxt_20260502_007_small_validation.py` | 已创建；纯标准库；待真实 Python 解释器运行 |
| 实现准备记录 | `06_计算实验/NXT-20260502-007_小规模脚本实现准备记录.md` | 已创建；记录覆盖范围、工具链状态、运行命令和 ESE/IVE/RD 判定 |
| 实验记忆更新 | `07_研究记忆/实验记忆.md` | 已记录 Python 入口为 WindowsApps 占位程序，作为实现环境 IVE |
| NXT 队列同步 | `08_agents工作流/下一轮任务包队列.md` | NXT-20260502-007 已改为 `进行中`，对应 report 改为 EVO-20260502-009 |
| RD 队列同步 | `08_agents工作流/研究债务队列.md` | 已追加 NXT-007 处理补充和 RND-20260502-009 阶段小结；未关闭债务 |

## 4. 脚本覆盖范围

| 模块 | 覆盖内容 | 边界 |
|---|---|---|
| 离散 DRCC 对照 | \(\delta_i^G\)、分数背包原问题、一维对偶、grid epigraph 目标值 | 小规模离散 oracle；不证明连续支撑精确性 |
| big-M 标签 | \(E_g(x)\ge B-\varepsilon_{\rm num}\) 的保守闭坏集标签函数 | 容差只用于脚本判定，不改变数学定义 |
| finite fiber A 类 | \(K=4\) 正交航向 + hover 的近碰撞 smoke search | 只能检查候选逻辑，不证明 16 候选上界 |
| finite fiber B 类 | 重复/退化航向 + hover 的近碰撞 smoke search | 用于寻找失败模式，不能验证上界成立 |

## 5. 工具链结果

| 工具 | 检查结果 | 影响 |
|---|---|---|
| `python` / `python3` | 当前为 WindowsApps 占位入口，未返回真实 Python 版本 | 本轮不能声称脚本已运行成功；NXT-007 保持 `进行中` |
| 外部求解器 | 未进入依赖检查 | 当前脚本不依赖外部求解器；后续若引入 SciPy/MILP 求解器需单独记录 |

### 5.1 工具配置申请状态（待用户确认）

| 项目 | 内容 |
|---|---|
| 申请对象 | 真实 Python 解释器，建议 Python 3.11 或 Python 3.12 |
| 触发原因 | NXT-20260502-007 脚本已准备，但当前 `python/python3` 为 WindowsApps 占位入口 |
| 服务任务 | 运行 `06_计算实验/scripts/nxt_20260502_007_small_validation.py` 并生成验证日志 |
| 当前依赖边界 | 脚本仅使用 Python 标准库；暂不申请 SciPy、MILP 求解器或商用许可证 |
| 可选处理方式 | 用户提供已有 Python 路径；用户确认后安装 Python；暂缓 NXT-007 真实运行 |
| 当前裁定 | 待用户确认；确认前不安装、不升级、不配置新工具 |

后续用户已确认使用本地 Anaconda 建立项目专属环境，真实运行结果另见 `EVO-20260502-010.md`。

## 6. 研究债务影响

| 债务 ID | 本轮动作 | 状态 |
|---|---|---|
| RD-20260501-002 | 完成小规模离散 DRCC 对照脚本准备 | 仍为 `进行中`；脚本未真实运行，不能关闭 |
| RD-20260502-003 | 完成 finite fiber A/B smoke search 脚本准备 | 仍为 `进行中` 与 `claim阻断`；尚未运行反例搜索 |

本轮无新增研究债务。

## 7. 记忆写回判定

| 字段 | 判定 |
|---|---|
| 记忆写回 | 更新实验记忆中的 Python 环境状态和 IVE 事件；不写入成功策略 |
| 是否触发 IDE | 否；本轮不产生新研究方向 |
| 是否触发 IVE | 是；实现环境缺口：当前 Python 入口不可用，导致脚本尚未完成真实运行 |
| 是否触发 ESE | 否；脚本文件已准备，但没有真实运行日志，不能写成可复用成功实验策略 |
| 是否只是研究债务 | 是；RD-20260501-002 与 RD-20260502-003 继续保留 |
| 证据来源 | EVO-20260502-009；NXT-20260502-007；实验记忆；证明策略记忆 |
| PI 边界 | 不得升级 C-009/C-011；不得关闭 `claim阻断` 债务；不得写入论文实验结果 |

## 8. Claim 防线

- C-009 保持 `待验证 / 反例优先`。
- C-011 保持 `仅有推导 / 待验证`。
- 本轮不修改 `00_总览索引/claim审批表.md`。
- 本轮不关闭 RD-20260501-002 或 RD-20260502-003。

## 9. 下一步

1. 用户确认后安装或指定真实 Python 解释器。
2. 运行：

```powershell
python 06_计算实验/scripts/nxt_20260502_007_small_validation.py --out 06_计算实验/results/nxt_20260502_007_results.json
```

3. 将 stdout 与结果 JSON 摘要写入 `06_计算实验/logs/`。
4. 再根据运行结果判断 ESE/IVE/RD 写回。

## 10. 非 claim 结论

- 本 report 不证明 C-009 或 C-011。
- 本 report 不说明 DRCC-MILP 已通过验证。
- 本 report 不产生正式 benchmark。
- 本 report 不关闭任何 `claim阻断` 债务。
