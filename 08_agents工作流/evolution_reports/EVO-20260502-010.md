# EVO-20260502-010：NXT-20260502-007 小规模脚本首次运行

## 1. 轮次信息

| 字段 | 内容 |
|---|---|
| 轮次 ID | EVO-20260502-010 |
| 日期 | 2026-05-02 |
| 输入触发 | 用户确认可使用本地 Anaconda 建立项目专属环境 |
| 主任务类型 | T4 计算实验与反例验证 |
| 关联 claim | C-009/C-011 |
| 调用角色 | 离散DRCC验证实验员；大规模MILP算法专家；反例搜索员；实验与Benchmark副导师；研究债务协调员 |
| 使用技能 | 手工项目角色审查；NXT-20260502-007 脚本 |
| 本轮边界 | 只完成小规模脚本首次运行；不生成正式 benchmark，不升级 claim，不关闭 `阻塞` 或 `claim阻断` 债务 |

## 2. 工具配置结果

| 项目 | 内容 |
|---|---|
| Conda 路径 | `C:\Users\dzw\anaconda3\Scripts\conda.exe` |
| 新建环境 | `wind-uav-drcc` |
| Python 版本 | Python 3.11.15 |
| 安装范围 | 只创建 conda 环境并安装 Python；未安装 SciPy、PuLP、Pyomo、Gurobi、CPLEX 或其他求解器 |
| 边界 | 后续若需要额外包、MILP 求解器或许可证，必须另行提出工具配置申请 |

## 3. 运行命令

```powershell
& 'C:\Users\dzw\anaconda3\Scripts\conda.exe' run -n wind-uav-drcc python 06_计算实验/scripts/nxt_20260502_007_small_validation.py --out 06_计算实验/results/nxt_20260502_007_results.json
```

stdout 已保存为：

```text
06_计算实验/logs/nxt_20260502_007_stdout.log
```

结果 JSON 已保存为：

```text
06_计算实验/results/nxt_20260502_007_results.json
```

## 4. 结果摘要

| 检查对象 | 结果摘要 | 解释边界 |
|---|---|---|
| DRCC-SMALL-001-no-bad | 分数背包、对偶、grid epigraph 均为 0，状态 `pass` | 无坏点小例子通过 |
| DRCC-SMALL-002-zero-radius | 三种计算均为 0.7，最大差距为 0，状态 `pass` | 零半径离散小例子通过 |
| DRCC-SMALL-003-fractional | 三种计算均为 0.7775，最大差距为 0，状态 `pass` | 分数预算小例子通过 |
| finite fiber A 类 | 正交航向 + hover 的 121 个采样点中未发现近碰撞 | 无反例不等于证明 16 候选上界 |
| finite fiber B 类 | 退化航向设置中发现 1 个近碰撞样本 | 作为失败模式线索，不能单独推出 C-009 结论 |

## 5. 研究债务影响

| 债务 ID | 本轮动作 | 状态 |
|---|---|---|
| RD-20260501-002 | 完成离散 DRCC 小规模对照脚本首次运行，三种计算在内置小例子上一致 | 仍为 `进行中`；需要角色审查、更多实例和边界检查后才能判断是否解除阻塞 |
| RD-20260502-003 | 完成 finite fiber A/B smoke search 首次运行，B 类退化航向出现近碰撞线索 | 仍为 `进行中` 与 `claim阻断`；需要反例搜索员与证明审计员继续审查 |

本轮无新增研究债务。

## 6. 记忆写回判定

| 字段 | 判定 |
|---|---|
| 记忆写回 | 更新实验记忆中的 `wind-uav-drcc` 环境、运行日志格式和首次脚本结果 |
| 是否触发 IDE | 否；本轮不产生新研究方向 |
| 是否触发 IVE | 是；finite fiber B 类退化航向近碰撞作为失败模式线索 |
| 是否触发 ESE | 是；项目专属 conda 环境、标准库脚本、JSON 输出和 stdout 日志格式可复用 |
| 是否只是研究债务 | 部分是；RD-20260501-002 与 RD-20260502-003 继续保留 |
| 证据来源 | EVO-20260502-010；NXT-20260502-007；`06_计算实验/results/nxt_20260502_007_results.json`；`06_计算实验/logs/nxt_20260502_007_stdout.log` |
| PI 边界 | 不得升级 C-009/C-011；不得关闭 `claim阻断` 债务；不得写入论文实验结果 |

## 7. Claim 防线

- C-009 保持 `待验证 / 反例优先`。
- C-011 保持 `仅有推导 / 待验证`。
- 本轮不修改 `00_总览索引/claim审批表.md`。
- 本轮不关闭 RD-20260501-002 或 RD-20260502-003。
- 本轮不产生正式 benchmark。

## 8. 下一步

1. 由离散DRCC验证实验员审查三个小例子的覆盖是否足以进入下一批实例设计。
2. 由反例搜索员审查 B 类退化航向近碰撞是否构成 C-009 的正式反例候选。
3. 若需要引入 SciPy、PuLP、Pyomo、Gurobi、CPLEX 或其他求解器，必须另行提出工具配置申请。

